const SiteExtra = ({title}) => {

    return (
        <>
            <h1>{title}</h1>
            <div>

            </div>
        </>
    )

}

SiteExtra.defaultProps = {
    title: 'This is Site Extra!'
}

export default SiteExtra;

